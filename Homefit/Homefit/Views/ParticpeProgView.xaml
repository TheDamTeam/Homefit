<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Homefit.Views.ParticpeProgView">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="progView" ItemsSource="{Binding ProgNutritionList}" SelectedItem="{Binding ProgSelected}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" HorizontalOptions="Fill" >
                                <Label Text="{Binding ProgrammeName}" HorizontalOptions="CenterAndExpand"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Text="Select" HorizontalOptions="CenterAndExpand" Command="{Binding GetSelectedProg}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>