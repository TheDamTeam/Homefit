<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:Homefit.Views.Controls"
             mc:Ignorable="d"
             x:Class="Homefit.Views.InscriptionView">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Nom" Text="{Binding Nom}"/>
            <Entry Placeholder="Prenom" Text="{Binding Prenom}"/>
            <DatePicker Date="{Binding DateNaiss}"/>
            <Entry Placeholder="Poids" Text="{Binding Poids}"/>
            <Entry Placeholder="Taille" Text="{Binding Taille}"/>
            <!--<Entry Placeholder="Sexe" Text="{Binding Sexe}"/>-->
            <StackLayout Orientation="Horizontal">
                <Switch IsToggled="{Binding Sexe}"/>
                <Label Text="{Binding SexeText}" VerticalOptions="CenterAndExpand"/>
            </StackLayout>
            <Entry Placeholder="Email" Text="{Binding Email}"/>
            <Entry Placeholder="Password" IsPassword="True" Text="{Binding Password}"/>
            <Button Text="SIGN UP" BackgroundColor="HotPink" TextColor="White" CornerRadius="30" Command="{Binding InscriptionButtonClickedCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>